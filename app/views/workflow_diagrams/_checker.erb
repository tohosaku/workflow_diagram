<%= tag.div :class => [:checker, (:hidden if hidden)], :data => { "visibility-target" => "element"} do %>
  <%= title %>
  <ul>
    <% obj.each do |e| %>
      <%= menu_item e %>
    <% end %>
  </ul>
<% end %>
